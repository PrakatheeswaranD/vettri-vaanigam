# dependencies
node_modules/
.pnpm-store/

# env
.env
.env.local
.env.*.local

# build output
dist/
build/
.turbo/
*.tsbuildinfo

# local dev database (PGlite data directory — see scripts/db-server.mjs)
.dbdata/
.dbdata-verification-*/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / OS junk
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# test / coverage
coverage/

# Prisma
apps/api/prisma/dev.db

# PGlite data directories. `.dbdata` was already ignored; the local
# variant was not, and a commit swept ~970 database files into the repo.
.dbdata-local/
**/.dbdata-local/

# Vite/Vitest write these transient config shims next to the real config.
*.timestamp-*.mjs
