{
  "name": "@razorgrowth/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared request/response schemas (Zod) and DTO contracts between apps/api and apps/web.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint --config ../../eslint.config.js src"
  },
  "dependencies": {
    "@razorgrowth/domain": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
